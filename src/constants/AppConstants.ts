export enum API_STATES {
  LOADING = "loading",
  SUCCESS = "success",
  FAILED = "failed",
}

export const HOME_PAGE_TABS_DATA = {
  NOW_PLAYING: { title: "NOW PLAYING", viewMoreLink: "/now-playing" },
};


export const ROUTE_CONSTANTS = {
  NOW_PLAYING : '/now-playing',
  HOME : '/'
}

export const MOVIE_CARD_WIDTH = {
  HOME_PAGE: 200,
  CATEGORY_PAGE: 300
}